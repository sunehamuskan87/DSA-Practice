Brute: squaring each array elements and sorting at the end -> O(n log n)

Optimal:
class Solution {
    public int[] sortedSquares(int[] nums) {
        int[] ans = new int[nums.length];
        
        int idx = ans.length-1;
        int i = 0;
        int j = nums.length-1;
        
        while(i<=j){
            int left = Math.abs(nums[i]);
            int right = Math.abs(nums[j]);
            
            if(left > right){
                ans[idx--] = left*left;
                i++;
            }
            else{
                ans[idx--] = right*right;
                j--;
            }
        }
    return ans;
    }
}
