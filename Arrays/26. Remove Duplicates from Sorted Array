Brute : we can use hashset and put the set values back to array and return idx+1 value
optimal : using 2 pointer

class Solution {
    public int removeDuplicates(int[] nums) {
        int idx = 0;
        for(int i=0;i<nums.length;i++){
            if(i==0 || nums[i] != nums[i-1]){
                nums[idx++] = nums[i];
            }
        }
        return idx;
    }
}
